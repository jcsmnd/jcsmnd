{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "sudo apt-get update sudo apt-get install curl curl -sL https://deb.nodesource.com/setup_14.x -o nodesource_setup.sh sudo bash nodesource_setup.sh sudo apt-get install nodejs sudo apt-get install build-essential sudo npm install -g pm2 sudo pm2 start index.js --watch sudo pm2 startup systemd sudo a2enmod proxy sudo a2enmod proxy_http sudo service apache2 restart",
  "main": "index.js",
  "dependencies": {
    "express": "^4.17.1",
    "rimraf": "^3.0.2",
    "socket.io": "^4.0.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jcsmnd",
  "license": "ISC"
}
